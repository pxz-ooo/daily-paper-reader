---
title: "From Rigid to Dynamic: Entropy-Guided Adaptive Inference for Long-Context LLMs"
title_zh: 从刚性到动态：基于熵引导的长上下文LLM自适应推理
authors: "Zhanchao Xu, Haoyang Li, Qingfa Xiao, Fei Teng, Chen Jason Zhang, Lei Chen, Qing Li"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09508v1"
tags: ["query:inf-accel"]
score: 9.0
evidence: 基于熵引导的自适应KV缓存压缩方法用于长上下文LLM推理
tldr: 现有长上下文LLM推理的稀疏注意力和KV压缩方法通常采用固定稀疏模式或均匀预算，忽略了不同注意头间的行为差异。本文发现注意头存在熵值始终接近零的“刚性头”和熵值波动显著的“动态头”，且分布随上下文变化。提出EntropyInfer框架，在预填充阶段利用注意力熵自适应分配计算，在解码阶段利用生成标记识别并保留关键KV缓存。在Llama、Qwen、openPangu系列上，EntropyInfer显著优于SnapKV等基线，在超100k token场景下实现最高2.39倍端到端加速，性能退化极小，为长上下文推理提供了无需训练的加速方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09508v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1611, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1631, \"height\": 459, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1489, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 773, \"height\": 508, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1549, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 805, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09508v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 679, \"height\": 370, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09508v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 812, \"height\": 741, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09508v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 748, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09508v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1624, \"height\": 503, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09508v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1443, \"height\": 396, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.09508v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1475, \"height\": 394, \"label\": \"Table\"}]"
motivation: 现有长上下文LLM推理中，稀疏注意力和KV压缩采用固定模式，无法适应注意力头行为的上下文依赖性，导致效率低下。
method: 提出EntropyInfer框架，预填充阶段用注意力熵按头和段自适应分配计算，解码阶段利用生成token进行潜在KV缓存压缩。
result: 在Llama、Qwen、openPangu系列上，比SnapKV、AdaKV等基线端到端加速最高2.39倍，质量退化极小。
conclusion: 通过熵引导的动态计算分配和潜在KV压缩，有效提升了长上下文推理效率，且无需训练，具有实用价值。
---

## 摘要
现有的针对长上下文LLM推理的稀疏注意力和KV缓存压缩方法通常应用固定的稀疏模式或跨所有注意力头的统一预算，忽视了头和上下文之间注意力行为的显著变化。我们观察到注意力头之间有两种不同的熵模式：刚性头，其熵在不同输入段中保持接近零；动态头，其熵波动显著。关键是，这些类型的分布是上下文相关的，无法离线预先确定。因此，我们提出EntropyInfer，一个无训练框架，在预填充阶段使用注意力熵在单个头和段的粒度上自适应地分配计算。对于解码，我们引入了一种潜在的KV缓存压缩方案，利用生成的输出token（而不是仅预填充token）来识别和保留最关键的缓存条目。在Llama、Qwen和openPangu模型系列上的大量实验表明，EntropyInfer始终优于SnapKV、AdaKV和CritiPrefill等基线，在超过10万token时实现高达2.39倍的端到端加速，且与全注意力相比质量下降极小。代码已在https://github.com/SHA-4096/EntropyInfer发布。

## Abstract
Existing sparse attention and KV cache compression methods for long-context LLM inference typically apply fixed sparsity patterns or uniform budgets across all attention heads, overlooking the substantial variation in attention behavior among heads and contexts. We observe two distinct entropy patterns among attention heads: Rigid Heads, whose entropy stays near zero across input segments, and Dynamic Heads, whose entropy fluctuates significantly. Crucially, the distribution of these types is context-dependent and cannot be predetermined offline. We therefore propose EntropyInfer, a training-free framework that uses attention entropy to adaptively allocate compute at the granularity of individual heads and segments during prefilling. For decoding, we introduce a latent KV cache compression scheme that leverages generated output tokens, rather than prefill tokens alone, to identify and retain the most critical cache entries. Extensive experiments on Llama, Qwen and openPangu model series show that EntropyInfer consistently outperforms baselines including SnapKV, AdaKV, and CritiPrefill, achieving up to 2.39$\times$ end-to-end speedup beyond 100k tokens with minimal quality degradation compared to full attention. The code is released in https://github.com/SHA-4096/EntropyInfer.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文大语言模型（LLM）推理面临两大成本中心——预填充阶段二次复杂度的注意力计算和解码阶段线性增长的 KV 缓存大小。现有稀疏注意力和 KV 压缩方法（如 MInference、SnapKV、AdaKV、CritiPrefill）采用**固定稀疏模式**或**统一预算**，忽略了不同注意力头之间的行为差异以及行为随上下文变化的动态性。
- **研究动机**：通过分析注意力熵（attention entropy）发现两个关键现象：① 注意力头可被划分为**刚性头**（熵始终接近 0）和**动态头**（熵显著波动）；② 同一头在不同输入下可能归入不同类别，因此**离线头分类无效**。现有方法无法在线适应这些变化。
- **整体含义**：提出一个**无需训练**的框架 EntropyInfer，利用注意力熵在线信息，在预填充阶段按头和段自适应分配计算预算，在解码阶段利用生成 token 重新评估 KV 缓存重要性，从而实现高效推理且保持生成质量。

### 2. 论文提出的方法论：核心思想、关键技术细节

#### 核心思想
- 利用注意力熵作为在线信号，对**刚性头**分配固定最小预算，对**动态头**根据相邻段之间的熵波动动态调整预算（波动越大，预算越多）。
- 解码阶段延迟压缩至生成少量 token 后，使用**生成的 output token** 构造观测窗口，重新排序并选取最重要的 KV 缓存条目，修正预填充到解码阶段注意力模式的偏移。

#### 关键技术细节
1. **熵引导的稀疏预填充**（Prefill）
   - 为降低计算，将注意力矩阵分块，构造**观测注意力矩阵** `A_obs`（使用查询和键分段后的最大/最小值计算）。
   - 计算每行（即每段）的注意力熵 `H(ai:)`。
   - 根据最大熵是否超过阈值 `et=10^-5` 区分头类型：
     - **刚性头**：为所有段分配固定基数预算 `Bh`。
     - **动态头**：为第一段分配 `Bh`，后续段按熵波动比例调整预算 `B_i = B_{i-1} + α * Bh * (|e_i - e_{i-1}|/e_{i-1} - Δt)`，并裁剪在 `[Bh, 3*Bh]` 内。
   - 最终只计算预算对应的 top-k 块注意力。

2. **潜在 KV 缓存压缩**（Decode）
   - 不立即压缩，而是先生成 `Nd` 个 token。
   - 以生成的 token 的查询（`Q_W`）与原始 key 矩阵计算注意力得分，按全局重要性（累加各行）选取 top-`Bd` 个 key/value 作为压缩后的缓存。
   - 算法 4 详细描述了该过程。

3. **复杂度分析**：总计算复杂度近似为 `O((1/(LS*LB) + (3/(2*LS*LB))*log(Bh/LB) ) * N^2 + 3*Bh*N)`，由于二次项系数极小，实际上近线性。

### 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：
  - **LongBench**：16 个任务，涵盖问答（NarrativeQA、HotpotQA 等）、摘要（MultiNews、GovReport 等）、检索（PassageRetrieval）、代码（LCC、RepoBench-P）等。
  - **InfiniteBench**：10 个超长上下文任务（>100k tokens），如 Retrieve.PassKey、En.Dia、En.Sum、Math.Find 等。
  - **LoCoMo**（用于 openPangu 多轮对话评估）。
- **基线方法**：
  - **全注意力（Base）**（无加速）。
  - **SnapKV**、**AdaKV**（KV 压缩方法，budget=1024）。
  - **CritiPrefill**（稀疏预填充方法，预算=2048）。
- **评估指标**：F1、Rouge-L、Accuracy、Edit Sim、Exact Match 等，按任务类型选用。
- **模型**：Llama-3.1-8B-Instruct、Qwen2.5-7B-Instruct、openPangu-Embedded-1B 和 7B。

### 4. 资源与算力

- 使用 **单张 NVIDIA H100 80G GPU**，搭配 **192GB CPU 内存**和 **8 个 CPU 核心**。
- 文中**未明确**说明训练时长（因为方法是无训练的 fine-tuning-free，仅需推理）。
- 实验为推理加速评估，无额外训练开销。

### 5. 实验数量与充分性

- **主实验**：两个长表（LongBench 16 个数据集 + InfiniteBench 10 个任务）对比 4 种基线（Base、SnapKV、AdaKV、CritiPrefill），在每个模型上均进行了测试。
- **消融实验**：3 种设置（Ours、Ours w/o SP（禁用稀疏预填充）、Ours w/o LD（禁用潜在解码）），验证各模块贡献。
- **参数敏感性实验**：
  - 段大小（128/256/512）和块大小（8/16/32）的网格搜索（4 个任务）。
  - 预填充基数预算和 KV 缓存预算的影响（2 个数据集）。
- **扩展实验**：在 openPangu-1B/7B 上验证效率和效果（LongBench 和 LoCoMo）。
- **充分性评价**：实验覆盖了不同任务类型、不同模型系列、不同上下文长度（4K~140K），并与多个 SOTA 方法公平比较（采用相同预算设置）。消融和参数分析充分，结论可信。

### 6. 论文的主要结论与发现

- EntropyInfer 在**端到端延迟评估**中相比全注意力实现最大 **2.39 倍加速**，在 140K 上下文时仍显著优于所有基线。
- **生成质量**：在 LongBench 和 InfiniteBench 上平均得分与全注意力接近（甚至有时略高），且**始终优于** SnapKV、AdaKV、CritiPrefill 等基线。
- **在线熵信号**可以有效地区分刚性头和动态头，动态预算分配比固定预算更鲁棒。
- 解码阶段使用**生成 token 重新排序** KV 缓存显著提升了压缩后的生成质量（对比 Ours w/o LD）。
- 方法对**段大小和预算参数**具有较好的稳定性，尤其预填充基数预算影响小。

### 7. 优点：方法或实验设计的亮点

- **无需训练**：可直接作为 drop-in 注意力模块替换，易部署。
- **完全在线自适应**：利用运行时熵信息动态调整预算，无需离线 profiling。
- **解码阶段创新**：首次在 KV 压缩中利用生成 token 的注意力信号（而非仅预填充信号），更贴合实际生成需求。
- **全面的对比与消融**：涵盖主流基线、多种模型（开源的 Llama、Qwen、openPangu）及超长上下文场景，实验设计严谨。
- **高效与质量平衡**：在获得显著加速的同时几乎不损失性能，部分任务甚至优于全注意力。

### 8. 不足与局限

- **短上下文加速有限**：论文承认在短上下文（<4K）时，因引入观测注意力计算和熵 profile 的开销，性能提升不明显。但随上下文增长，优势凸显。
- **未覆盖更大模型**：实验仅包含 1B/7B/8B 模型，未在 70B 或更大模型上验证（虽然方法通用）。
- **未讨论极端情况下可能导致的信息丢失**：解码阶段压缩虽使用生成 token，但若生成的 token 质量不高（如早期生成错误），可能错误评估关键 KV 条目。
- **阈值依赖**：刚性与动态头的熵阈值 `10^-5` 是经验设定，可能对不同模型或任务需微调。
- **硬件依赖**：实验仅在一个 GPU 配置上进行，未研究多 GPU 或更复杂部署场景。
- **未提供 openPangu 在多轮对话 LoCoMo 上的效率数据**（仅给了表 4 的效果，效率见图 7 但仅对比了加速比，未详细列出延迟值）。

（完）
