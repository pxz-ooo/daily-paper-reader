---
title: "BitLM: Unlocking Multi-Token Language Generation with Bitwise Continuous Diffusion"
title_zh: BitLM：利用按位连续扩散解锁多Token语言生成
authors: "Shaobin Zhuang, Yuang Ai, Jiaming Han, Xiaohui Li, Huaibo Huang, Xiangyu Yue, Xuefeng Hu, Kun Xu, Yali Wang, Hao Chen"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11577v1"
tags: ["query:inf-accel"]
score: 9.0
evidence: 通过扩散实现多标记预测以加速解码
tldr: 自回归语言模型每次只生成一个token，但自然语言常以多token单元（如短语）表达意义，导致预训练表现和推理效率受限。BitLM提出将每个token编码为固定长度二进制码，并利用轻量扩散头在块内并行去噪多个token，同时保持跨块的从左到右因果注意力。该方法将token生成转化为紧凑二进制空间中的迭代提交，实现了更高效的预训练和更快的推理速度。实验表明，一次一个token并非语言模型的本质要求，改变这一接口可得到更强更快的模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11577v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 678, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.11577v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1456, \"height\": 644, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.11577v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1414, \"height\": 875, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.11577v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1448, \"height\": 356, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.11577v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 683, \"height\": 364, \"label\": \"Table\"}]"
motivation: 现有自回归模型一次生成一个token的瓶颈限制了表达能力和推理吞吐量，需要更高效的多token并行生成方法。
method: BitLM将token表示为二进制码，通过轻量扩散头在保持因果注意力的同时并行去噪多个token。
result: 相比传统自回归模型，BitLM实现更高效预训练和显著更快的推理速度，且不牺牲因果结构。
conclusion: 一次一个token不是语言模型的基本要求，改变生成接口可得到更强更快的模型。
---

## 摘要
自回归语言模型一次生成一个Token，但自然语言本质上是由多Token单元构成的，包括短语、n-gram和搭配，这些共同承载含义。这种单Token瓶颈限制了模型在预训练期间的表达能力以及推理时的吞吐量。现有的补救措施，如推测解码或基于扩散的语言模型，要么未解决根本瓶颈，要么牺牲了语言建模所需的因果结构。我们提出BitLM，一种将每个Token表示为固定长度二进制码的语言模型，并采用轻量级扩散头在每个块内并行去噪多个Token。关键在于，BitLM在块间保留从左到右的因果注意力，同时在每个块内进行联合词汇决策，结合了自回归建模的可靠性和迭代细化的并行性。通过用按位去噪取代大词汇表softmax，BitLM将Token生成重新定义为紧凑二进制空间中的迭代承诺，从而实现更高效的预训练和显著更快的推理，同时不改变使语言模型有效的因果基础。我们的结果表明，一次生成一个Token的范式并非基本要求，而是一种接口选择，改变它可以产生更强大、更快的语言模型。我们希望BitLM能为下一代语言模型架构指明有前景的方向。

## Abstract
Autoregressive language models generate text one token at a time, yet natural language is inherently structured in multi-token units, including phrases, n-grams, and collocations that carry meaning jointly. This one-token bottleneck limits both the expressiveness of the model during pre-training and its throughput at inference time. Existing remedies such as speculative decoding or diffusion-based language models either leave the underlying bottleneck intact or sacrifice the causal structure essential to language modeling. We propose BitLM, a language model that represents each token as a fixed-length binary code and employs a lightweight diffusion head to denoise multiple tokens in parallel within each block. Crucially, BitLM preserves left-to-right causal attention across blocks while making joint lexical decisions within each block, combining the reliability of autoregressive modeling with the parallelism of iterative refinement. By replacing the large-vocabulary softmax with bitwise denoising, BitLM reframes token generation as iterative commitment in a compact binary space, enabling more efficient pre-training and substantially faster inference without altering the causal foundation that makes language models effective. Our results demonstrate that the one-token-at-a-time paradigm is not a fundamental requirement but an interface choice, and that changing it can yield a stronger and faster language model. We hope BitLM points toward a promising direction for next-generation language model architectures.

---

## 论文详细总结（自动生成）

# BitLM 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：传统自回归语言模型每次生成一个 token，但自然语言本质上是以多 token 单元（如短语、n-gram、固定搭配）来表达意义。这种逐个 token 的接口限制了预训练期间模型的表达能力和推理时的吞吐量。
- **现有解决方案的不足**：推测解码、非自回归方法、扩散语言模型等要么未解决根本瓶颈，要么牺牲了语言建模关键的因果结构。
- **整体含义**：BitLM 提出，一次生成一个 token 并非语言模型的本质要求，而是当前输出接口（大词汇表 softmax）的历史选择。通过改变符号输出空间的几何结构（将 token 表示为二进制码，用扩散并行生成多个 token），可以同时获得更强表达能力和更快推理速度，且不破坏因果结构。这为下一代语言模型架构设计开辟了新方向。

## 2. 方法论

### 核心思想
- 将每个 token ID 映射为固定长度二进制码（B 位），将词汇级分类问题转化为紧凑二进制空间中的迭代去噪问题。
- 保持因果语言模型主干（用于上下文计算），但用轻量扩散头取代传统的 softmax 头，在每个块内联合去噪多个 token 的二进制码，实现块内并行生成。
- 使用**块因果注意力掩码**：块内 token 可以相互关注，块间保持从左到右因果依赖。

### 关键技术细节
- **二进制编码**：词汇大小 V，二进制码长度 B = ⌈log₂V⌉（实验中使用 B=18）。每个 token ID 以 binary 表示，映射为 {−1, +1}^B 的向量，即二进制超立方体的顶点。
- **输入 MLP**：将二进制码提升到主干隐藏维度，替换传统的 token embedding 查找。
- **扩散过程**：对每个目标块，采样时间步 t ~ U[0,1] 和噪声 ϵ ~ N(0, I)，通过直线插值构建带噪声的连续二进制状态：A_t = (1−t)A_0 + tϵ。扩散头预测干净块 Â_0，训练用简单的 x0-预测 MSE 损失。
- **条件注入**：扩散头使用自适应层归一化（AdaLN）将主干上下文状态和时间步嵌入融入去噪过程。
- **块因果注意力掩码**：定义块索引，所有同一块内的 token 都可以相互注意，不同块之间只有低块（过去块）可以注意高块（未来块）。当块大小 m=1 时退化为标准因果注意力。
- **训练流程**（算法 1）：输入序列分词 → 二进制编码 → 填充至块大小的倍数 → 分割为块 → MLP 提升 → 块因果 LLM 计算上下文 → 对每个块采样噪声，扩散头预测干净二进制码 → 累积损失。
- **推理流程**（算法 2）：从噪声初始化下一个块，通过 K 步 ODE 去噪（论文使用 K=15），最后通过 sign 投影回 {−1, +1} 并解码为 token ID。然后通过一次主干前向传播（含 KV cache 缓存）更新上下文，继续生成下一个块。

### 公式说明
- 块级因子分解：p(y₁:ₗ) = ∏ p(y⁽ⁿ⁾ | y⁽<ⁿ⁾)，每块含 m 个 token。
- 每个块内不分解为独立位置和独立比特，而是由扩散头联合建模所有 m×B 个变量的依赖关系。

## 3. 实验设计

### 数据集
- **预训练**：FineWeb 的子集，共 350B token，训练 1 个 epoch。
- **微调**：XSum（极端摘要数据集）训练集。

### Benchmark 与对比方法
- 评测指标：ROUGE-1、ROUGE-2、ROUGE-L。
- 对比方法：
  - Lead-3（抽取式基线）
  - PTGEN（指针生成网络）
  - PTGEN+COV（带覆盖机制的 PTGEN）
  - BitLM 自身变体：8B 模型，比较了使用标准语言模型头（LM Head，即 softmax）的版本和扩散头（Diff. Head）的版本，均在预训练（PT）和微调（FT）下报告结果。

### 主要实验结果
- 微调后的 8B BitLM（扩散头）在 XSum 上 ROUGE-1/2/L 达到 26.05/6.44/20.12，超过 Lead-3 基线，但低于 PTGEN 系列。
- 论文认为结果有前景但尚不成熟，指出模型学到了非平凡的摘要映射，但精确词汇实现能力仍有差距。

### 消融与参数分析
- **去噪步数 K** 和 **无分类器引导强度（cfg）** 的消融：在 XSum 上，K=15、cfg=9 时性能最优。
- **模型规模实验**：预训练损失随模型增大（0.6B, 1.7B, 4B, 8B）持续下降，展示了良好的可扩展性。

## 4. 资源与算力

- 论文未明确说明 GPU 型号、数量、训练时长等具体计算资源细节。
- 仅提及使用了 AdamW 优化器，学习率 1e-4，β₁=0.9，β₂=0.95，每条训练序列长度 16384 token，采用序列打包（packing）提高效率。
- 模型规模从 0.6B 到 8B，预训练数据量为 350B token，预计需要大量 GPU 算力，但具体数字未给出。

## 5. 实验数量与充分性

- 实验数量相对有限：
  - 主要展示在 XSum 上的微调结果（仅一个下游任务）和预训练损失曲线（四种规模）。
  - 对推理超参数（去噪步数、cfg）进行了消融（一个图，一个指标下的调参）。
  - 缺乏与其他先进扩散语言模型（如 LLaDA、Block Diffusion）或加速方法（如 Medusa、投机解码）的直接对比；论文自述当前结果尚不成熟。
- 充分性评估：
  - **优势**：展示了从预训练到微调的完整流程，验证了二进制扩散接口的可扩展性。
  - **不足**：实验覆盖面窄（仅一项下游任务）；缺乏与现有方法的公平对比（同等规模、同等训练数据下的标准自回归模型结果未给出）；未在多个任务、多种解码加速场景下验证并行生成的实际加速比（仅定性描述快）。整体实验尚显初步。

## 6. 主要结论与发现

1. **二进制码替代词汇 softmax 是可行的**：BitLM 可在数十亿参数规模下被成功预训练，并且通过有监督微调能在摘要任务上获得合理性能。
2. **块因果并行生成是模型原生的**：由于扩散头在块内联合去噪，多 token 并行生成成为模型自身属性，无需后处理加速技巧。
3. **模型表现出良好的可扩展性**：随模型增大训练损失持续下降。
4. **去噪步数和引导强度是重要的性能杠杆**：适中的步数（K=15）和较高的引导强度（cfg=9）在 XSum 上效果最好。
5. **当前结果仍有较大提升空间**：在需要精确词汇实现的任务上，二进制扩散方案尚不如成熟的指针网络或 softmax 自回归模型，但方向有潜力。

## 7. 优点

- **概念创新**：将符号生成从离散的分类问题重新定义为连续二进制空间中的迭代细化，并从视觉生成领域（BitDance、Analog Bits）借鉴经验迁移到文本，思路新颖。
- **因果结构保持**：与传统非自回归或扩散语言模型不同，BitLM 保留了跨块的因果依赖，同时放宽了块内的因果约束，是一种平衡并行性和因果性的优雅设计。
- **轻量扩散头**：扩散头架构简单（基于 BitDance），主要贡献不在于复杂网络设计，而在于输出接口的几何重构，使加速成为内生特性。
- **训练稳定**：使用简单的 x0-预测目标和标准 AdamW 优化器，没有特殊的训练技巧，可扩展性好。
- **概念桥梁**：将自回归语言建模、扩散生成和并行解码三大方向联系起来，提供了统一视角。

## 8. 不足与局限

- **下游性能尚不成熟**：在 XSum 上的结果低于经典指针生成网络，缺少与同等规模标准自回归模型（如同架构 Qwen-3 直接用 softmax）的公平对比，难以判断性能损失的具体来源。
- **实验覆盖不足**：
  - 仅测试一个下游任务（摘要），未涵盖常见基准如语言建模困惑度、问答、翻译等。
  - 未报告实际推理速度的加速比（仅定性说“显著更快”），缺乏延迟或吞吐量的定量测量。
  - 缺乏与现有并行解码方法（Medusa、投机解码）或扩散语言模型（LLaDA、Block Diffusion）的 head-to-head 比较。
- **二进制码是固定而非学习的**：作者承认使用固定二进制 ID（如整数二进制表示），而非学习语义代码本，这可能限制了表示效率。未来工作可探索学习二进制码。
- **潜在偏差与公平性**：仅使用 FineWeb 的 350B 子集和 XSum 数据集，可能存在数据偏见，论文未讨论生成内容的安全性与公平性风险（尽管有伦理声明）。
- **应用限制**：块大小 m 固定为 4，未探索自适应块大小或可变块长的生成，可能限制灵活性和质量。对于需要精确符合格式的文本（如代码、表单），二进制噪声可能引入更多错误。
- **推理复杂度**：虽然生成一个块可以减少主干前向次数，但扩散头需要多次迭代（K=15），实际端到端加速可能不如推测解码等方法显著，需具体量化。

（完）
